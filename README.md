Project php_learn
Cloud Storage

Облачное хранилище для файлов различного типа. Максимальный размер файла 2 Гб.
Реализовано:

- шаринг папок и файлов как для пользователей, так и для групп;
- смена пароля пользователем;
- редактирование пользователей администратором;
- редактирование групп администратором;
- сброс пароля (default = 'user123') или задание нового.

init.sql - развертывание бд для работы
config/database.php - настройки для подключения к бд
public/index.php - точка входа

для входа:
login - admin
passwd - 123F

# Аутентификация пользователя 
___

## POST /register - *регистрация пользователя*

*Происходит проверка на обязательные параметры `email`, `login`, `password` - yнедолжны быть пустыми.*
#### ***Отправка параметров***
- `email` - email пользователя,
- `login` - login пользователя,
- `password` - пароль пользователя.

***Формат***

```json
{
    "email": "value",
    "login": "value",
    "password": "value"
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
- `success` - true,
- `message` - сообщение об успешной регистрации,
- `redirect` - '/login' адрес перенаправления для входа.
- 
***Формат***

```json
{
    "success": "true",
    "message": "Пользователь зарегистрирован. Пожалуйста, войдите.",
    "redirect": "/login"
}
```

##### **`(при ошибке)`** ❌
- `http_response_code(400)` - сервер не может или не будет обрабатывать запрос из-за чего-то, что воспринимается как ошибка клиента,
- `success` - false,
- `message` - сообщение об ошибке.

***Формат***

```json
{
    "success": "false",
    "message": "Пользователь с таким email или login уже существует"
}
```

##### **`(при пустых данных)`** 💡
- `http_response_code(400)` - сервер не может или не будет обрабатывать запрос из-за чего-то, что воспринимается как ошибка клиента,
- `success` - false,
- `message` - сообщение об ошибке.

***Формат***

```json
{
    "success": "false",
    "message": "Email, Login и пароль обязательны"
}
```
## POST /login - *аутентификация пользователя*

#### ***Отправка параметров***
- `email_or_login` - email или логин пользователя,
- `password` - пароль пользователя.

***Формат***

```json
{
    "email_or_login": "value",
    "password": "value"
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
Создает токен аутентификации `auth_token`. ⚠️
- `success` - true,
- `id` - id пользователя,
- `redirect` - '/' адрес перенаправления.
- 
***Формат***

```json
{
    "success": "true",
    "id": "$user->id",
    "redirect": "/"
}
```

##### **`(при ошибке)`** ❌
- `http_response_code(500)` - на сервере произошла ошибка, в результате которой он не может успешно обработать запрос,
- `success` - false,
- `message` - сообщение об ошибке.

***Формат***

```json
{
    "success": "false",
    "message": "Ошибка при создании токена"
}
```

##### **`(при неудачной авторизации)`** 💡
- `http_response_code(401)` - неавторизованный пользователь,
- `success` - false,
- `message` - сообщение об ошибке.

***Формат***

```json
{
    "success": "false",
    "message": "Неверный логин или пароль."
}
```

## GET /logout - *выход пользователя*
- параметров для отправки нет,
- извлекает токен `'auth_token'` из запроса,
- удаляет токен на сервере и на клиенте,

#### **`в случае успеха`**  ✅
- `http_response_code(200)`,
- `success` - true,
- `message` - сообщение об успехе.
***Формат***
```json
{
    "success": "true",
    "message": "Вы успешно вышли из системы."
}
```

##### **`при ошибке`** ❌
- `http_response_code(500)` - на сервере произошла ошибка, в результате которой он не может успешно обработать запрос,
- `success` - false,
- `message` - сообщение об ошибке.

***Формат***

```json
{
    "success": "false",
    "message": "Ошибка при выходе из системы."
}
```

## GET /reset_password - *сброс пароля пользователя*
Сброс пароля пользователей доступен только для admin (проверка по role === 1).

#### ***Отправка параметров***
- `user_id` - id пользователя которому производится сброс пароля,
- `new_password` - новый пароль пользователя.(если поле пустое - `user123`) 

***Формат***

```json
{
    "user_id": "value",
    "new_password": "value" ?? ""
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
- `success` - true,
- `message` - 'Пароль изменен.',
- 
***Формат***

```json
{
    "success": "true",
    "message": "Пароль изменен.",
}
```

##### **`(при ошибке)`** ❌
- http_response_code(500),
- `success` - false,
- `error` - 'Ошибка при смене пароля.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Ошибка при смене пароля.",
}
```

## POST /change_password - *сброс своего пароля пользователя*
Сброс своего пароля пользователя. 

#### ***Отправка параметров (все поля обязательны)*** 
- `current_password` - текущий пароль пользователя,
- `new_password` - новый пароль пользователя (не менее 6 символов),
- `confirm_new_password` - подтверждение нового пароля пользователя.

***Формат***

```json
{
    "current_password": "value",
    "new_password": "value",
    "confirm_new_password": "value" 
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
- `success` - true,
- `message` - 'Пароль успешно изменён.',
- 
***Формат***

```json
{
    "success": "true",
    "message": "Пароль успешно изменён.",
}
```

##### **`(при наличии пустого поля)`** ⚠️
- http_response_code(400),
- `success` - false,
- `error` - 'Все поля обязательны.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Все поля обязательны.",
}
```
##### **`(при несовпадении подтверждения пароля)`** ⚠️
- http_response_code(400),
- `success` - false,
- `error` - 'Новый пароль и подтверждение не совпадают.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Новый пароль и подтверждение не совпадают.",
}
```
##### **`(валидация пароля больше 6 символов)`** ⚠️
- http_response_code(400),
- `success` - false,
- `error` - 'Новый пароль должен быть не менее 6 символов.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Новый пароль должен быть не менее 6 символов.",
}
```
##### **`(проверка на соответствие текущего пароля)`** ⚠️
- http_response_code(400),
- `success` - false,
- `error` - 'Текущий пароль неверен',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Текущий пароль неверен",
}
```
##### **`(при ошибке сохранения нового пароля)`** ❌
- http_response_code(500),
- `success` - false,
- `error` - 'Ошибка при изменении пароля.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Ошибка при изменении пароля.",
}
```



# USERS 
___
## GET /user/search/{email} - *поиск пользователя по email*

#### ***Отправка параметров***
- `email` - email искомого пользователя ,

***Формат***

```json
{
    "email": "value",
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
- http_response_code(200),
- `success` - true,
- `user` - объект пользователя с 'общими' полями ,
- 
***Формат***

```json
{
    "success": "true",
    "user": [
        {
            "id": 5,
            "login": "user2",
            "email": "2@1.ru",
            "role": 0,
        }
    ]
}
```

##### **`(при отсутствии email в запросе)`** ⚠️
- http_response_code(500),
- `success` - false,
- `message` - 'email отсутствует.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "email отсутствует.",
}
```

##### **`(при ошибке)`** ❌
- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с '$email' не найден.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Пользователь с '$email' не найден.",
}
```

## GET /users/list - *получение списка пользователей*

#### ***Отправка параметров***
- `email` - email искомого пользователя ,

***Формат***

```json
{
    "email": "value",
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
- http_response_code(200),
- `success` - true,
- `user` - возвращает данные пользователя,
- 
***Формат***

```json
{
    "success": "true",
    "user": [
        {
            "id": 5,
            "login": "user2",
            "email": "2@1.ru",
            "role": 0,
        }
    ]
}
```

##### **`(при отсутствии email в запросе)`** ⚠️
- http_response_code(500),
- `success` - false,
- `message` - 'email отсутствует.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "email отсутствует.",
}
```

##### **`(при ошибке)`** ❌
- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с `$email` не найден.',

***Формат***

```json
{
    "success": "false",
    "message": "Пользователь с `$email` не найден.",
}
```

## GET /users/get/{id} - *получение пользователя по id*

#### ***Отправка параметров***
- `id` - user_id искомого пользователя,

***Формат***

```json
{
    "id": "value",
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
- http_response_code(200),
- `success` - true,
- `user` - возвращает данные пользователя,
- 
***Формат***

```json
{
    "success": "true",
    "user": [
        {
            "id": 5,
            "login": "user2",
            "email": "2@1.ru",
            "role": 0,
        }
    ]
}
```

##### **`(при отсутствии id в запросе)`** ⚠️
- http_response_code(500),
- `success` - false,
- `message` - 'id отсутствует.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "id отсутствует.",
}
```

##### **`(при ошибке)`** ❌
- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с `id` не найден.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Пользователь с '$id' не найден.",
}
```

## PUT /users/update - *обновление данных пользователя*
Обновление только разрешенных полей у авторизованного пользователя (login и(или) email).
Запрещенные параметры  - id, role, password_hash, created_at.
#### ***Отправка параметров***
- `data` - ассоциативный массив с параметрами "ключ-значение",

***Формат***

```json
{
    data = [
        "login": "value",
        "email": "value"
    ]
}
```

#### ***Возвращаемые данные***
##### **`(при успешном запросе)`** ✅
- http_response_code(200),
- `success` - true,
- `message` - Данные обновлены.,
- 
***Формат***

```json
{
    "success": "true",
    "message": "Данные обновлены."
}
```

##### **`(при отсутствии данных в запросе)`** ⚠️
- http_response_code(400),
- `success` - false,
- `message` - 'Данные для обновления не предоставлены.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Данные для обновления не предоставлены.",
}
```

##### **`(при отправке запрещенных парметров в запросе)`** ⚠️
- http_response_code(403),
- `success` - false,
- `message` - 'Доступ запрещен.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Доступ запрещен.",
}
```

##### **`(при наличии такого парметра у другого пользователя)`** ❌
Проверка на уникальность параметра. (login, email)
- http_response_code(400),
- `success` - false,
- `message` - 'Значение `$value` уже используется.',
- 
***Формат***

```json
{
    "success": "false",
    "message": "Значение '$value' уже используется.",
}
```

# ADMIN
___

GET /admin/users/list

GET /admin/users/get/{id}

DELETE /admin/users/delete/{id}

PUT /admin/users/update/{id}

FILES

GET /files/list

GET /files/get/{id}

POST /files/add

PUT /files/rename

DELETE /files/remove/{id}

DIRECTORIES

POST /directories/add

PUT /directories/rename

GET /directories/get/{id}

DELETE /directories/delete/{id}

FILES/SHARE

GET /files/share/{id}

PUT /files/share/{id}/{user_id}

DELETE /files/share/{id}/{user_id}
=======
init.sql - развертывание бд для работы

config/database.php - настройки для подключения к бд 

public/index.php - точка входа 

для входа: 
login - admin
passwd - 123
