# Cloud Storage REST/API

## *Облачное хранилище для файлов различного типа. Максимальный размер файла 2 Гб.*
#### *В проекте используется:*
- `PHP 8.3.19`,
- `.htaccess` для веб-сервера apache,
- `база данных` - `MySQL-8.2`.

#### *Реализовано:*
- шаринг папок и файлов как для пользователей, так и для групп;
- смена пароля пользователем;
- редактирование пользователей администратором;
- редактирование групп администратором;
- сброс пароля (default = 'user123') или задание нового.
- отправка сообщения на email пользователя для сброса пароля.

#### *Необходимые файлы для старта проекта:*
- `init.sql` - развертывание БД для работы,
- `config/database.php` - настройки для подключения к БД,
- `public/index.php` - точка входа,
- `composer.json` - для установки библиотек, используемых в проекте. (в файле указаны фактические версии, работающие в проекте)
    ```
    команда для установки зависимостей:
      composer install
    ```
- необходимо создать в корне проекта файл `.env` (параметры для PHPMailer):
    ```
      MAIL_HOST=`smtp.mail.ru` (SMTP-сервер, через который отправляется почта)
      MAIL_PORT=`587` (порт SMTP-сервера)
      MAIL_USERNAME=`user@mail.ru` (логин для входа в почтовый аккаунт)
      MAIL_PASSWORD=`"value"` (пароль и почтового аккаунта для разрешения удаленного подключения)
      MAIL_ENCRYPTION=`tls` (тип шифрования соединения в зависимости от порта)
      MAIL_FROM_ADDRESS=`user@mail.ru` (email, от которого будет отправлено письмо)
      MAIL_FROM_NAME=`CloudStorage` (имя отправителя, которое увидит получатель)
    ```

#### Во всех запросах реализована проверка на авторизованного пользователя, `за исключением`:
```
  ('GET', '') - главная страница,
  ('POST', 'login') - авторизация пользователя,
  ('POST', 'register') - регистрация пользователя,
  ('GET', 'reset_password') - отправка ссылки для сброса пароля на почту пользователя.
```

#### *Для входа в проект после создания БД и настройки подключения к БД:*
- `login` - admin
- `password` - 123

# Аутентификация пользователя

---

## POST /register - *регистрация пользователя*

Происходит проверка на обязательные параметры `email`, `login`, `password` - не должны быть пустыми.

#### **Отправка параметров**📩

- `email` - email пользователя,
- `login` - login пользователя,
- `password` - пароль пользователя.

**Формат**

```json
{
  "email": "value",
  "login": "value",
  "password": "value"
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- `success` - true,
- `message` - сообщение об успешной регистрации,
- `redirect` - '/login' адрес перенаправления для входа.
  
 **Формат**

```json
{
  "success": true,
  "message": "Пользователь зарегистрирован. Пожалуйста, войдите.",
  "redirect": "/login"
}
```

##### **`(при ошибке)`** ❌

- `http_response_code(400)` - сервер не может или не будет обрабатывать запрос из-за чего-то, что воспринимается как ошибка клиента,
- `success` - false,
- `message` - сообщение об ошибке.

**Формат**

```json
{
  "success": false,
  "message": "Пользователь с таким email или login уже существует"
}
```

##### **`(при пустых данных)`** 💡

- `http_response_code(400)` - сервер не может или не будет обрабатывать запрос из-за чего-то, что воспринимается как ошибка клиента,
- `success` - false,
- `message` - сообщение об ошибке.

**Формат**

```json
{
  "success": false,
  "message": "Email, Login и пароль обязательны"
}
```

## POST /login - *аутентификация пользователя*

#### **Отправка параметров**📩

- `email_or_login` - email или логин пользователя,
- `password` - пароль пользователя.

**Формат**

```json
{
  "email_or_login": "value",
  "password": "value"
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

Создает токен аутентификации `auth_token`. ⚠️

- `success` - true,
- `id` - id пользователя,
- `redirect` - '/' адрес перенаправления.
  
**Формат**

```json
{
  "success": true,
  "id": "$user->id",
  "redirect": "/"
}
```

##### **`(при ошибке)`** ❌

- `http_response_code(500)` - на сервере произошла ошибка, в результате которой он не может успешно обработать запрос,
- `success` - false,
- `message` - сообщение об ошибке.

**Формат**

```json
{
  "success": false,
  "message": "Ошибка при создании токена"
}
```

##### **`(при неудачной авторизации)`** 💡

- `http_response_code(401)` - неавторизованный пользователь,
- `success` - false,
- `message` - сообщение об ошибке.

**Формат**

```json
{
  "success": false,
  "message": "Неверный логин или пароль."
}
```

## GET /logout - *выход пользователя*

- параметров для отправки нет,
- извлекает токен `'auth_token'` из запроса,
- удаляет токен на сервере и на клиенте,

#### **`в случае успеха`** ✅

- `http_response_code(200)`,
- `success` - true,
- `message` - сообщение об успехе.
**Формат**

```json
{
  "success": true,
  "message": "Вы успешно вышли из системы."
}
```

##### **`при ошибке`** ❌

- `http_response_code(500)` - на сервере произошла ошибка, в результате которой он не может успешно обработать запрос,
- `success` - false,
- `message` - сообщение об ошибке.

**Формат**

```json
{
  "success": false,
  "message": "Ошибка при выходе из системы."
}
```

## GET /reset_password - *отправка ссылки для сброса пароля пользователя*
Реализована логика: 
 - отправка запроса не чаще 15 мин,
 - максимальное количество запросов в сутки = 5. 

#### **Отправка параметров**📩

- **Query Parameters**:
  - `email` - email пользователя

#### **`в случае успеха`** ✅

- `http_response_code(200)`,
- `success` - true,
- `message` - "Ссылка для сброса пароля отправлена на ваш email."
**Формат**

```json
{
  "success": false,
  "message": "Ссылка для сброса пароля отправлена на ваш email."
}
```

##### **`(при отсутствии email в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Email не указан.',
- **Формат**

```json
{
  "success": false,
  "message": "Email не указан."
}
```

##### **`(при отсутствии пользователя с таким email )`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с таким email не найден.',
- **Формат**

```json
{
  "success": false,
  "message": "Пользователь с таким email не найден."
}
```

##### **`(при попытке отправки запроса реже 15 мин)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Слишком частые запросы. Повторите попытку через 15 минут.',
- **Формат**

```json
{
  "success": false,
  "message": "Слишком частые запросы. Повторите попытку через 15 минут."
}
```

##### **`(при попытке отправки запросов больше 5 в сутки)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Превышено количество запросов на сегодня. Попробуйте завтра.',
- **Формат**

```json
{
  "success": false,
  "message": "Превышено количество запросов на сегодня. Попробуйте завтра."
}
```

##### **`при ошибке`** ❌

- `http_response_code(500)` - на сервере произошла ошибка, в результате которой он не может успешно обработать запрос,
- `success` - false,
- `message` - 'Внутренняя ошибка сервера'
- `debug` - 'Сообщение об ошибке'

**Формат**

```json
{
  "success": false,
  "message": "Внутренняя ошибка сервера",
  "debug": 'Сообщение об ошибке'
}
```

## POST /change_password - *смена своего пароля пользователя*

Смена своего пароля пользователя.

#### **Отправка параметров (все поля обязательны)**📩

- `current_password` - текущий пароль пользователя,
- `new_password` - новый пароль пользователя (не менее 6 символов),
- `confirm_new_password` - подтверждение нового пароля пользователя.

**Формат**

```json
{
  "current_password": "value",
  "new_password": "value",
  "confirm_new_password": "value"
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- `success` - true,
- `message` - 'Пароль успешно изменён.',
- **Формат**

```json
{
  "success": true,
  "message": "Пароль успешно изменён."
}
```

##### **`(при наличии пустого поля)`** ⚠️

- http_response_code(400),
- `success` - false,
- `error` - 'Все поля обязательны.',
- **Формат**

```json
{
  "success": false,
  "message": "Все поля обязательны."
}
```

##### **`(при несовпадении подтверждения пароля)`** ⚠️

- http_response_code(400),
- `success` - false,
- `error` - 'Новый пароль и подтверждение не совпадают.',
  
**Формат**

```json
{
  "success": false,
  "message": "Новый пароль и подтверждение не совпадают."
}
```

##### **`(валидация пароля больше 6 символов)`** ⚠️

- http_response_code(400),
- `success` - false,
- `error` - 'Новый пароль должен быть не менее 6 символов.',
  
**Формат**

```json
{
  "success": false,
  "message": "Новый пароль должен быть не менее 6 символов."
}
```

##### **`(проверка на соответствие текущего пароля)`** ⚠️

- http_response_code(400),
- `success` - false,
- `error` - 'Текущий пароль неверен',

**Формат**

```json
{
  "success": false,
  "message": "Текущий пароль неверен"
}
```

##### **`(при ошибке сохранения нового пароля)`** ❌

- http_response_code(500),
- `success` - false,
- `error` - 'Ошибка при изменении пароля.',
  
 **Формат**

```json
{
  "success": false,
  "message": "Ошибка при изменении пароля."
}
```

# USERS

---

## GET /users/search/{email} - *поиск пользователя по email*

#### **Отправка параметров**📩

- `email` - email искомого пользователя ,

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `user` - объект пользователя с 'общими' полями ,
- **Формат**

```json
{
  "success": true,
  "user": [
    {
      "id": 5,
      "login": "user2",
      "email": "2@1.ru",
      "role": 0
    }
  ]
}
```

##### **`(при отсутствии email в запросе)`** ⚠️

- http_response_code(500),
- `success` - false,
- `message` - 'email отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "email отсутствует."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с '$email' не найден.',
- **Формат**

```json
{
  "success": false,
  "message": "Пользователь с '$email' не найден."
}
```

## GET /users/list - *получение списка пользователей*

#### **Отправка параметров**📩

- `email` - email искомого пользователя ,

**Формат**

```json
{
  "email": "value"
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `user` - возвращает данные пользователя,
- **Формат**

```json
{
  "success": true,
  "user": [
    {
      "id": 5,
      "login": "user2",
      "email": "2@1.ru",
      "role": 0
    }
  ]
}
```

##### **`(при отсутствии email в запросе)`** ⚠️

- http_response_code(500),
- `success` - false,
- `message` - 'email отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "email отсутствует."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с `$email` не найден.',

**Формат**

```json
{
  "success": false,
  "message": "Пользователь с `$email` не найден."
}
```

## GET /users/get/{id} - *получение пользователя по id*

#### **Отправка параметров**📩

- `id` - id искомого пользователя,

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `user` - возвращает данные пользователя,
- **Формат**

```json
{
  "success": true,
  "user": [
    {
      "id": 5,
      "login": "user2",
      "email": "2@1.ru",
      "role": 0
    }
  ]
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(500),
- `success` - false,
- `message` - 'id отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "id отсутствует."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с `id` не найден.',
- **Формат**

```json
{
  "success": false,
  "message": "Пользователь с '$id' не найден."
}
```

## PUT /users/update - *обновление данных пользователя*

Обновление только разрешенных полей у авторизованного пользователя (login и(или) email).
Запрещенные параметры - id, role, password_hash, created_at.

#### **Отправка параметров**📩

- `data` - ассоциативный массив с параметрами "ключ-значение",

**Формат**

```json
{
  "data": {
    "login": "value",
    "email": "value"
  }
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - Данные обновлены.,
- **Формат**

```json
{
  "success": true,
  "message": "Данные обновлены."
}
```

##### **`(при отсутствии данных в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Данные для обновления не предоставлены.',
- **Формат**

```json
{
  "success": false,
  "message": "Данные для обновления не предоставлены."
}
```

##### **`(при отправке запрещенных парметров в запросе)`** ⚠️

- http_response_code(403),
- `success` - false,
- `message` - 'Доступ запрещен.',
- **Формат**

```json
{
  "success": false,
  "message": "Доступ запрещен."
}
```

##### **`(при наличии такого парметра у другого пользователя)`** ❌

Проверка на уникальность параметра. (login, email)

- http_response_code(400),
- `success` - false,
- `message` - 'Значение `$value` уже используется.',
- **Формат**

```json
{
  "success": false,
  "message": "Значение '$value' уже используется."
}
```

# ADMIN

---

Для всех последующих методов проверяется авторизация admin по токену `auth_token`⚠️

## GET /admin/users/list - *получение списка пользователей для администратора*

#### **Отправка параметров**📩

- **Query Parameters**:
  - `id` - id пользователя

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `usersList` - список пользователей,
- **Формат**

```json
{
  "success": true,
  "usersList": [
    {
      "id": "value",
      "email": "value",
      "login": "value"
    }
  ]
}
```

## GET /admin/users/get/{id} - *получение всех данных пользователя по id*

#### **Отправка параметров**📩
- `id` - id пользователя

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `user` - возвращает данные пользователя,
- **Формат**

```json
{
  "success": true,
  "user": [
    {
      "id": "value",
      "login": "value",
      "email": "value",
      "password_hash": "value",
      "role": "value",
      "created_at": "value"
    }
  ]
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(500),
- `success` - false,
- `message` - 'id пользователя отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "id пользователя отсутствует."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(400),
- `success` - false,
- `message` - 'Пользователь с `id` `value` не найден.',
- **Формат**

```json
{
  "success": false,
  "message": "Пользователь с 'id' `value` не найден."
}
```

## DELETE /admin/users/delete/{id} - *удаление пользователя по id*

#### **Отправка параметров**📩
- `id` - id пользователя

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - "Пользователь успешно удален."
  **Формат**

```json
{
  "success": true,
  "message": "Пользователь успешно удален."
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(500),
- `success` - false,
- `message` - 'id пользователя отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "id пользователя отсутствует."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Пользователь с `id` `value` не найден.',
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка при удалении пользователя."
}
```

## PUT /admin/users/update/{id} - *обновление данных пользователя по id*

Запрещенные параметры для редактирования `id`, `password`.
Уникальные параметры `login`, `email`.

#### **Отправка параметров**📩

- `id` - id пользователя
- dataUser - данные для изменения пользователя

**Формат**

```json
{
  "dataUser": [
    {
      "login": "value",
      "email": "value",
      "role": "0" || "1"
    }
  ]
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - "Данные пользователя успешно обновлены."
  **Формат**

```json
{
  "success": true,
  "message": "Данные пользователя успешно обновлены."
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(500),
- `success` - false,
- `message` - 'id пользователя отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "id пользователя отсутствует."
}
```

##### **`(при отсутствии данных в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Данные для обновления отсутствуют.',
- **Формат**

```json
{
  "success": false,
  "message": "Данные для обновления отсутствуют."
}
```

##### **`(при отправке данных запрещенных для редактирования)`** ⚠️

- http_response_code(403),
- `success` - false,
- `message` - 'Поля запрещены для редактирования.',
- **Формат**

```json
{
  "success": false,
  "message": "Поля запрещены для редактирования."
}
```

##### **`(при совпадении уникальных значений с другими пользователями)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Значение $key - $value уже используется.',
- **Формат**

```json
{
  "success": false,
  "message": "Значение $key - $value уже используется."
}
```

##### **`(при не валидности email)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Email не валиден.',
- **Формат**

```json
{
  "success": false,
  "message": "Email не валиден."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка при обновлении данных.',
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка при обновлении данных."
}
```

## POST /admin/users/reset_password/{id} - *сброс пароля пользователя по id*

Новый пароль должен быть не менее 6 символов.

#### **Отправка параметров**📩

- `id` - id пользователя
- `new_password` - опционально `("" = "user123")`

  **Формат**

```json
{
    "new_password": "value" ?? "",
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - "Пароль успешно изменен."
  **Формат**

```json
{
  "success": true,
  "message": "Пароль успешно изменен."
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(500),
- `success` - false,
- `message` - 'id пользователя отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "id пользователя отсутствует."
}
```

##### **`(при отсутствии пользователя с таким id)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Пользователь не найден.',
- **Формат**

```json
{
  "success": false,
  "message": "Пользователь не найден."
}
```

##### **`(валидация пароля)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Новый пароль должен быть не менее 6 символов',
- **Формат**

```json
{
  "success": false,
  "message": "Новый пароль должен быть не менее 6 символов"
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка при смене пароля.',
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка при смене пароля."
}
```

# FILES
___

**Максимальный размер файлов 2Gb.**

**Допустимые типы файлов:**
- `Изображения`: 
    - 'image/jpeg',
    - 'image/png',
    - 'image/gif',
    - 'image/webp',
- `Текст`: 
    - 'text/plain',
- `PDF`:
    - 'application/pdf',
- `Документы Word`: 
    - .doc
    - .docx
- `Электронные таблицы Excel`: 
    - .xls
    - .xlsx
- `Презентации PowerPoint`: 
    - .ppt
    - .pptx
- `Видео — общие (веб)`:
    - 'video/mp4',
    - 'video/webm',
    - 'video/ogg',
- `Видео — для фильмов (популярные контейнеры и кодеки)`:
    - .mkv — самый популярный контейнер для фильмов
    - .mov — Apple QuickTime, часто используется в киноиндустрии
    - .wmv — Microsoft Windows Media Video
    - .avi — старый, но всё ещё распространённый формат
    - .mpg, .mpeg — MPEG-1/2, иногда используется для DVD
    - .flv — устаревший, но встречается
    - .m4v — похож на mp4, часто используется Apple
- `Аудио`:
    - 'audio/mpeg',      // .mp3
    - 'audio/wav',
    - 'audio/ogg',
    - 'audio/aac',
    - 'audio/flac',      // .flac — без потерь, популярен у аудиофилов
    - 'audio/x-ms-wma',  // .wma — Microsoft Windows Media Audio
    - 'video/3gpp',      // .3gp — мобильные видео
    - 'video/3gpp2',     // .3g2 — улучшенная версия 3GPP

___

## GET /files/list - *получение списка файлов*


#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅
- http_response_code(200),
- `success` - true,
- `filesList` - "список файлов"
  **Формат**

```json
{
    "success": true,
    "filesList": [
        {
            "id": 1,
            "original_name": "Test.docx",
            "filename": "6968b6ca02176_test.docx",
            "size": 13226,
            "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            "user_id": 1,
            "folder_id": null,
            "created_at": "2026-01-15 12:43:38"
        },
        {
            "id": 2,
            "original_name": "test_exel.xlsx",
            "filename": "6968bb4e8003c_test_exel.xlsx",
            "size": 8377,
            "mime_type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            "user_id": 2,
            "folder_id": null,
            "created_at": "2026-01-15 13:02:54"
        },

    ]
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка сервера.',
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка сервера."
}
```

## GET /files/list/get/{id} - *получение списка файлов пользователя по id*

#### **Отправка параметров**📩
- `id` - id пользователя

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅
- http_response_code(200),
- `success` - true,
- `filesList` - "список файлов"
  **Формат**

```json
{
    "success": true,
    "filesList": [
        {
            "id": 1,
            "original_name": "Test.docx",
            "filename": "6968b6ca02176_test.docx",
            "size": 13226,
            "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            "user_id": `id пользователя`,
            "folder_id": null,
            "created_at": "2026-01-15 12:43:38"
        },
        {
            "id": 2,
            "original_name": "test_exel.xlsx",
            "filename": "6968bb4e8003c_test_exel.xlsx",
            "size": 8377,
            "mime_type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            "user_id": `id пользователя`,
            "folder_id": null,
            "created_at": "2026-01-15 13:02:54"
        },

    ]
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка сервера.',
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка сервера."
}
```

## GET /files/get/{id} - *получение данных о файле*

#### **Отправка параметров**📩
- `id` - id файла

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `file` - "Данные о файле"
  **Формат**

```json
{
    "success": true,
    "file": [
        {
            "id": 1,
            "original_name": "Test.docx",
            "filename": "6968b6ca02176_test.docx",
            "size": 13226,
            "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            "user_id": 1,
            "folder_id": null,
            "created_at": "2026-01-15 12:43:38"
        }
    ]
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'id файла отсутствует.',
- **Формат**

```json
{
  "success": false,
  "message": "id файла отсутствует."
}
```

##### **`(при отсутствии файла)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Файла не найдено.',
- **Формат**

```json
{
  "success": false,
  "message": "Файла не найдено."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка сервера.',
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка сервера."
}
```

## PUT /files/rename - *изменение имени файла*
Присутствует проверка на уникальность имени без учета регистра. ☝️

#### **Отправка параметров**📩
- `file_id` - id файла,
- `new_name` - новое название файла.

**Формат**

```json
{
    "file_id": "1",
    "new_name": "value",
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - "Файл успешно переименован на 'new_name'."
  
**Формат**

```json
{
  "success": true,
  "message": "Файл успешно переименован на 'new_name'."
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID файла не указан',
  
**Формат**

```json
{
  "success": false,
  "message": "ID файла не указан"
}
```

##### **`(при отсутствии нового имени в запросе)`** ⚠️
- http_response_code(400),
- `success` - false,
- `message` - 'Новое имя файла не может быть пустым.',
  
- **Формат**

```json
{
  "success": false,
  "message": "Новое имя файла не может быть пустым."
}
```

##### **`(проверка на уникальность без учета регистра)`** ⚠️
- http_response_code(409),
- `success` - false,
- `message` - 'Файл с таким именем у вас уже есть - $fileName.',
- **Формат**

```json
{
  "success": false,
  "message": "Файл с таким именем у вас уже есть - $fileName."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка при обновлении файла.',
  
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка при обновлении файла."
}
```

## DELETE /files/remove/{id} - *удаление файла по id*
Присутствует проверка права пользователя (`admin` разрешено).☝️
Удаление только из авторизованного пользователя.

#### **Отправка параметров**📩
- `id` - id файла


#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - "Файл успешно удален."
- 
  **Формат**

```json
{
  "success": true,
  "message": "Файл успешно удален."
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID файла не указан',
- 
- **Формат**

```json
{
  "success": false,
  "message": "ID файла не указан"
}
```

##### **`(при отсутствии прав)`** ⚠️
- http_response_code(403),
- `success` - false,
- `message` - 'Нет прав на удаление файла.',
- 
- **Формат**

```json
{
  "success": false,
  "message": "Нет прав на удаление файла."
}
```

##### **`(при отсутствии файла)`** ⚠️
- http_response_code(403),
- `success` - false,
- `message` - 'Файл не найден.',
- 
- **Формат**

```json
{
  "success": false,
  "message": "Файл не найден."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Внутренняя ошибка сервера',
- 
- **Формат**

```json
{
  "success": false,
  "message": "Внутренняя ошибка сервера"
}
```

## POST /files/add - *загрузка файла на сервер*
Загружает файл на сервер с добавлением приставки в название для создания уникальности в случае совпадения имен файлов у разных пользователей. 
Хранение файлов всех пользователей организовано в папке `/uploads`.
В базе данных создана структура принадлежности к папкам файлов, которые в них лежат, как в дереве файловой системы.
Принадлежность файла пользователю определяется по авторизованному пользователю который загружает файл.
Проводится проверка на уникальность имени внутри пользователя без учета регистра.
Также отправляется на сервер файл выбранный в `<input type="file" name="file" required>`

#### **Отправка параметров**📩
- `folder_id` - id папки в которой будет размещен файл (если файл в корне то `folder_id = ''` ).

**Формат**

```json
{
  "folder_id": "value",
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - "Файл $fileName успешно загружен."
- 
  **Формат**

```json
{
  "success": true,
  "message": "Файл $fileName успешно загружен."
}
```

##### **`(при отсутствии файла при отправке)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'Для загрузки обязателен файл.',
- **Формат**

```json
{
  "success": false,
  "message": "Для загрузки обязателен файл."
}
```

##### **`(при проверке на уникальность имени файла)`** ⚠️
- http_response_code(409),
- `success` - false,
- `message` - 'Файл с именем $fileName уже существует.',
- **Формат**

```json
{
  "success": false,
  "message": "Файл с именем $fileName уже существует."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка загрузки файла на сервер.',
- **Формат**

```json
{
  "success": false,
  "message": "Ошибка загрузки файла на сервер."
}
```

## GET /files/download/{id} - *скачивание файла по id*

#### **Отправка параметров**📩
- `id` - id файла


#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- header('Content-Type: ')
- header("Content-Disposition: ")
- header('Content-Length: ')

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID файла не передано.',
- 
- **Формат**

```json
{
  "success": false,
  "message": "ID файла не передано."
}
```

##### **`(при отсутствии файла)`** ⚠️
- http_response_code(404),
- `success` - false,
- `message` - 'Файл не найден на сервере.',
- 
- **Формат**

```json
{
  "success": false,
  "message": "Файл не найден на сервере."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Внутренняя ошибка сервера',
- 
- **Формат**

```json
{
  "success": false,
  "message": "Внутренняя ошибка сервера"
}
```


# DIRECTORIES
___

## POST /directories/add - *создание директории*
Реализована проверка на уникальность имени директории в области видимости родительской.

#### **Отправка параметров**📩
- `name` - имя новой директории,
- `parent_id` - id родительской папки (если в корне то `folder_id = ''` )

**Формат**

```json
{
  "name": "value",
  "parent_id": "1",
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - 'Папка создана успешно.',
 
**Формат**

```json
{
  "success": true,
  "message": "Папка создана успешно."
}
```

##### **`(при проврке на уникальность)`** ⚠️
- http_response_code(409),
- `success` - false,
- `message` - 'Папка с таким именем у вас уже есть - $name.',

**Формат**

```json
{
  "success": false,
  "message": "Папка с таким именем у вас уже есть - $name."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка при создании папки.',

**Формат**

```json
{
  "success": false,
  "message": "Ошибка при создании папки."
}
```

## PUT /directories/rename - *изменение имени директории*
**Реализовано:**
- Проверка прав доступа;
- Проверка на уникальность;

#### **Отправка параметров**📩
- `id` - id папки;
- `new_name` - новое имя директории. 

- **Формат**

```json
{
  "id": "1",
  "new_name": "value"
}
```

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - 'Папка успешно переименована на '$folderNewName'',
- 
- **Формат**

```json
{
  "success": true,
  "message": "Папка успешно переименована на '$folderNewName'"
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID папки не указан',
- 
- **Формат**

```json
{
  "success": false,
  "message": "ID папки не указан"
}
```

##### **`(при отсутствии нового имени папки)`** ⚠️
- http_response_code(404),
- `success` - false,
- `message` - 'Новое имя папки обязательно',
- 
- **Формат**

```json
{
  "success": false,
  "message": "Новое имя папки обязательно"
}
```

##### **`(при отсутствии прав)`** ❌

- http_response_code(409),
- `success` - false,
- `message` - 'Нет прав на переименование папки.',
 
**Формат**

```json
{
  "success": false,
  "message": "Нет прав на переименование папки."
}
```
##### **`(при проверке на уникальность)`** ❌

- http_response_code(409),
- `success` - false,
- `message` - 'Папка с таким именем уже существует.',
  
**Формат**

```json
{
  "success": false,
  "message": "Папка с таким именем уже существует."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка при обновлении папки.',
  
**Формат**

```json
{
  "success": false,
  "message": "Ошибка при обновлении папки."
}
```

## GET /directories/get/{id} - *получение содержимого директории*

#### **Отправка параметров**📩
- `id` - id папки;

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `data` - 'ассоциативный массив папок и файлов в директории',
 
**Формат**

```json
{
    "success": true,
    "data": {
        "folders": [
            {
                "id": 17,
                "name": "first",
                "parent_id": 4,
                "user_id": 1,
                "created_at": "2026-01-18 00:08:55"
            },
            {
                "id": 18,
                "name": "second",
                "parent_id": 4,
                "user_id": 1,
                "created_at": "2026-01-18 00:08:57"
            }
        ],
        "files": [
            {
                "id": 13,
                "original_name": "Test.doc",
                "filename": "696bfa91ec8e3_Test.doc",
                "size": 15017,
                "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
                "user_id": 1,
                "folder_id": 4,
                "created_at": "2026-01-18 00:09:37"
            },
        ]
    }
}
```

##### **`(при отсутсвии данных в директории)`** ⚠️
- http_response_code(200),
- `success` - true,
- `data` - '',

**Формат**

```json
{
    "success": true,
    "data": ""
}
```

##### **`(при отсутсвии директории)`** ⚠️
- http_response_code(404),
- `success` - false,
- `message` - 'Папки не найдено.',

**Формат**

```json
{
    "success": false,
    "message": "Папки не найдено."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Ошибка сервера',

**Формат**

```json
{
  "success": false,
  "message": "Ошибка сервера"
}
```

## DELETE /directories/delete/{id} - *удаление директории*
**Реализовано:**
- Проверка прав доступа;
- Рекурсивное удаление папок и файлов.

#### **Отправка параметров**📩
- `id` - id папки

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - 'Папка и всё её содержимое удалены.',
- 
- **Формат**

```json
{
  "success": true,
  "message": "Папка и всё её содержимое удалены."
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID папки не указан',
- 
- **Формат**

```json
{
  "success": false,
  "message": "ID папки не указан"
}
```

##### **`(при отсутствии директории)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Указанной папки не найдено.',
- 
- **Формат**

```json
{
  "success": false,
  "message": "Указанной папки не найдено."
}
```

##### **`(при отсутствии прав)`** ❌

- http_response_code(409),
- `success` - false,
- `message` - 'Нет прав на удаление папки.',
 
**Формат**

```json
{
  "success": false,
  "message": "Нет прав на удаление папки."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Внутренняя ошибка сервера.',
  
**Формат**

```json
{
  "success": false,
  "message": "Внутренняя ошибка сервера."
}
```


# FILES/SHARE
___

## GET /files/share/{id} - *получение пользователей расшаренного файла*
**Реализовано:**

#### **Отправка параметров**📩
- `id` - id файла

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `users` - 'список пользователей для файла',
 
- **Формат**

```json
{
    "success": true,
    "users": [
        {
            "id": 3,
            "login": "test",
            "email": "test@test.ru",
            "password_hash": "$2y$10$2Hzqi.wmDz9EjWWmPVWIzuntLBNL5hkUQ4LThUu4.RRVRgy3gOSXm",
            "role": 0,
            "created_at": "2026-01-18 01:17:59"
        },
        {
            "id": 2,
            "login": "testuser",
            "email": "test@mail.ru",
            "password_hash": "$2y$10$2EdF.riWuxMfsTGJLs63jOeLN3rCKWm0y1vpQWNTtSJSuLwbcBnXe",
            "role": 0,
            "created_at": "2026-01-16 17:12:26"
        }
    ]
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID файла обязательно.',

- **Формат**

```json
{
  "success": false,
  "message": "ID файла обязательно."
}
```

##### **`(при отсутствии файла)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Такого файла не существует.',

- **Формат**

```json
{
  "success": false,
  "message": "Такого файла не существует."
}
```

##### **`(при отсутствии шарингов с пользователями)`** ❌

- http_response_code(404),
- `success` - false,
- `message` - 'Файл ни с кем не расшарен.',
 
**Формат**

```json
{
  "success": false,
  "message": "Файл ни с кем не расшарен."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Внутренняя ошибка сервера.',
  
**Формат**

```json
{
  "success": false,
  "message": "Внутренняя ошибка сервера."
}
```

## PUT /files/share/{id}/{user_id} - *добавить доступ к файлу пользователю*
**Реализовано:**

#### **Отправка параметров**📩
- `id` - id файла,
- `user_id` - id пользователя,

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - 'Файл успешно поделён с указанными сущностями.',
- `details` - 'Сообщения при шаринге для групп (или шарим или обновляем доступ)'
  По умолчанию стоят права `read`.
 
- **Формат**

```json
{
    "success": true,
    "message": "Файл успешно поделён с указанными сущностями.",
    "details": ["Обновили уровень доступа для группы Developers"]
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID файла не передано.',

- **Формат**

```json
{
  "success": false,
  "message": "ID файла не передано."
}
```
##### **`(при отсутствии пользователя)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Указанного пользователя не найдено.',

- **Формат**

```json
{
  "success": false,
  "message": "Указанного пользователя не найдено."
}
```
##### **`(при отсутствии файла)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Указанного файла не найдено.',

- **Формат**

```json
{
  "success": false,
  "message": "Указанного файла не найдено."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Внутренняя ошибка сервера.',
  
**Формат**

```json
{
  "success": false,
  "message": "Внутренняя ошибка сервера."
}
```

## DELETE /files/share/{id}/{user_id} - *прекратить доступ к файлу пользователю или группе*
**Реализовано:**

#### **Отправка параметров**📩
- `id` - id файла,
- `user_id` - id пользователя,

#### **Возвращаемые данные**🔙

##### **`(при успешном запросе)`** ✅

- http_response_code(200),
- `success` - true,
- `message` - 'Шаринг удален',
 
- **Формат**

```json
{
    "success": true,
    "message": "Шаринг удален"
}
```

##### **`(при отсутствии id в запросе)`** ⚠️

- http_response_code(400),
- `success` - false,
- `message` - 'ID файла не передано.',

**Формат**

```json
{
  "success": false,
  "message": "ID файла не передано."
}
```

##### **`(при отсутствии прав на удаление)`** ⚠️

- http_response_code(409),
- `success` - false,
- `message` - 'Нет прав на удаление шаринга.',

**Формат**

```json
{
  "success": false,
  "message": "Нет прав на удаление шаринга."
}
```

##### **`(при отсутствии сущностей для расшаренного файла)`** ⚠️

- http_response_code(200),
- `success` - true,
- `message` - 'Файл не был расшарен для предоставленных сущностей.',

**Формат**

```json
{
  "success": true,
  "message": "Файл не был расшарен для предоставленных сущностей."
}
```

##### **`(при отсутствии файла)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Файла не найдено.',

**Формат**

```json
{
  "success": false,
  "message": "Файла не найдено."
}
```

##### **`(при отсутствии пользователя)`** ⚠️

- http_response_code(404),
- `success` - false,
- `message` - 'Пользователя не найдено.',

**Формат**

```json
{
  "success": false,
  "message": "Пользователя не найдено."
}
```

##### **`(при ошибке)`** ❌

- http_response_code(500),
- `success` - false,
- `message` - 'Внутренняя ошибка сервера.',
  
**Формат**

```json
{
  "success": false,
  "message": "Внутренняя ошибка сервера."
}
```


